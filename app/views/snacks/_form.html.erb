<%= form_with(model: snack, local: true) do |form| %>
  <% if snack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(snack.errors.count, "error") %> prohibited this snack from being saved:</h2>

      <ul>
      <% snack.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :seq %>
    <%= form.number_field :seq, id: :snack_seq %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :snack_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
