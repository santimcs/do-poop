<p id="notice"><%= notice %></p>

<h1>Lunches</h1>

<table>
  <thead>
    <tr>
      <th>Seq</th>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @lunches.each do |lunch| %>
      <tr>
        <td><%= lunch.seq %></td>
        <td><%= lunch.name %></td>
        <td><%= link_to_icon 'Show', lunch %></td>
        <td><%= link_to_icon 'Edit', edit_lunch_path(lunch) %></td>        
        <td><%= link_to_icon 'Destroy', lunch, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Lunch', new_lunch_path %>
